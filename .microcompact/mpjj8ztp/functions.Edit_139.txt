The file /home/project/src/pages/promotion/PromotionLiveTab.tsx has been updated. Made 1 replacement.

Here's the result of running `cat -n` on a snippet of the edited file:
  14→export default function PromotionLiveTab({ liveKpiData }: Props) {
  15→  if (!liveKpiData) return <div className="pdd-card text-center py-8 text-[var(--pdd-text-secondary)]">暂无直播推广数据</div>;
  16→
  17→  const kpiCards = [
  18→    { label: '推广花费', value: liveKpiData.totalCost, fmt: (v: number) => `¥${v.toFixed(0)}`, icon: DollarSign, color: 'pdd-danger' },
  19→    { label: '推广订单数', value: liveKpiData.promoOrders, fmt: (v: number) => v.toFixed(0), icon: ShoppingCart, color: '#1890ff' },
  20→    { label: '推广GMV', value: liveKpiData.promoGMV, fmt: (v: number) => `¥${v.toFixed(0)}`, icon: TrendingUp, color: '#52c41a' },
  21→    { label: '推广ROI', value: liveKpiData.roi, fmt: (v: number) => v.toFixed(2), icon: Target, color: '#722ed1' },
  22→    { label: '曝光量', value: liveKpiData.totalImpressions, fmt: (v: number) => v.toFixed(0), icon: Eye, color: '#1890ff' },
  23→    { label: '关注量', value: liveKpiData.followCount, fmt: (v: number) => v.toFixed(0), icon: Users, color: '#722ed1' },

<system-reminder>
🚫 EDIT LOOP WARNING: You've used Edit on "/home/project/src/pages/promotion/PromotionLiveTab.tsx" 8 times consecutively!

Your current approach is likely not working. You need to change strategy NOW.

⚠️ REQUIRED ACTIONS:
1. STOP retrying the same approach on "/home/project/src/pages/promotion/PromotionLiveTab.tsx"
2. Consider: is this modification actually critical? If not, SKIP it and move on to the next task
3. If you must fix it, try a fundamentally different approach — not a minor variation of what you've been doing
4. If you still can't get it right, tell the user what's blocking you and MOVE ON to the next step

⏰ 4 more attempt(s) before Edit to this file is AUTOMATICALLY BLOCKED. Focus on forward progress.
</system-reminder>