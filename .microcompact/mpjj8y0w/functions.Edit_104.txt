The file /home/project/src/pages/dashboard/DashboardDetailModal.tsx has been updated. Made 1 replacement.

Here's the result of running `cat -n` on a snippet of the edited file:
  59→                      return (
  60→                        <tr key={idx} className="border-b border-pdd-border hover:bg-[#1e2235] transition-colors">
  61→                          <td className="py-2 px-2 text-pdd-text-secondary">{idx + 1}</td>
  62→                          <td className="py-2 px-2 font-medium truncate max-w-[200px] text-pdd-text-primary" title={item.name}>{item.name}</td>
  63→                          <td className="py-2 px-2 text-right text-pdd-primary-light font-medium">{typeof item.value === 'number' ? item.value.toLocaleString() : item.value}</td>
  64→                          <td className="py-2 px-2 text-right text-pdd-text-secondary">{percent}%</td>
  65→                        </tr>
  66→                      );
  67→                    })}
  68→                  </tbody>