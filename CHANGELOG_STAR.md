# ⭐ 版本特别说明 — 商品列表UI大重构

> **标签**: `⭐`
> **提交**: `ca7674c`
> **日期**: 2026-06-25

---

## 一、本次改动核心

### 1. 商品列表布局重构（1394px 固定宽）

```
┌─────────────────┬────┬────┬──────────────────────────────────┬──────────┐
│   商品信息       │ 占比 │ 数值 │          滑动数据窗口          │  操作按钮  │
│   430px          │60px│36px│           798px                  │   70px    │
└─────────────────┴────┴────┴──────────────────────────────────┴──────────┘
                    ←──── 整行高度固定 100px ────→
```

- **整行高度锁定 100px**：商品信息、占比、数值、滑动窗口、操作按钮全部等高对齐
- **所有水平分割线全局对齐**：不再出现某列高某列矮的问题
- **占比/数值列为独立 td**：不再被数据窗口的横向滚动挤压

### 2. 占比列（60px）

| 内容 | 格式 |
|------|------|
| 销售占比 | `销售08.9%` |
| 推广占比 | `推广12.3%` |
| 订单占比 | `订单05.2%` |

- 两位数字 + 百分号，统一补零
- 60px 内紧凑排版，不溢出

### 3. 数值列（36px）

| 行 | 内容 |
|----|------|
| 第一行 | 目标 |
| 第二行 | 本期 |
| 第三行 | 同比 |

### 4. 数据窗口（798px，可横向滚动）

- 显示 8~14 列可选数据
- 每行三栏：**目标 / 本期 / 同比**
- 表头 28px + 内容三等分

### 5. 商品信息列（430px）

- 88px 商品图（可点击上传/预览）
- 商品名 + ID + 出单日期
- 成本 / 售价 / 利润
- 推广费 / ROI / 费比
- 标签系统（警报标签 + 系统标签，最多6个）
- 单品/总额模式切换
- 规格数展开

---

## 二、交互体验优化

### 悬停效果 — 「雾面」

- 鼠标悬停行时：`background-color: rgba(0,0,0,0.02)`（2% 黑色叠加）
- **过渡时长 1s**，缓动曲线 `cubic-bezier(0.22, 1, 0.36, 1)`
- 效果像一层薄雾轻轻覆盖，而非生硬的变色
- 展开行保持浅蓝底

### 数据窗口横向滑动 — 「物理惯性滚动」

- **≤9 列**：滚轮不拦截，正常上下翻页
- **>9 列**：滚轮捕获 + 惯性平滑横向滚动
  - 灵敏度 0.6（柔和不跳跃）
  - 限速 ±40px（防止飞出去）
  - 减速度 0.85（自然刹车感）
  - requestAnimationFrame 60fps 驱动

---

## 三、边框系统

| 位置 | 样式 |
|------|------|
| 模块外框（商品信息 / 占比+数值+窗口 / 操作按钮） | `1px solid #e5e7eb` |
| 行上下分割线 | `1px solid #e5e7eb` |
| 内部虚线（三行之间） | `1px dashed #e5e7eb` |
| 表头分割线 | `1px solid #e5e7eb` |

所有外框线统一粗细和颜色，模块之间通过间距自然区分。

---

## 四、技术改进

| 项目 | 说明 |
|------|------|
| React onWheel passive 限制 | 使用 addEventListener + `{passive: false}` 绕过 |
| `overflow-y: clip` | 替代 `overflow-y: hidden`，避免浏览器误创建滚动容器 |
| 去除 `overscroll-behavior: contain` | 让滚轮在 ≤9 列时正常穿透到页面 |
| 标签系统 | 16 类系统标签 + 5 类告警标签，支持显隐/筛选 |
| 数据列管理 | 单品/总额模式各自独立配置，最多 8 列，可自由拖选 |

---

## 五、如何恢复到此版本

```bash
# 查看标签
git tag -l

# 切换到 ⭐ 版本
git checkout ⭐

# 如果后续需要继续开发
git checkout master
```
