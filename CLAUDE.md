# CLAUDE.md

## 沟通语言
- **必须使用中文**与用户进行所有沟通，包括回复、解释、代码注释等。

## 项目概述
- 拼多多商家数据分析工具「店分析」
- 技术栈：React 18 + TypeScript + Webpack 5 + Tailwind CSS v3
- 数据解析：PapaParse (CSV) + xlsx (Excel)
- 图表：Recharts
- 动画：Framer Motion + dnd-kit

## 注意事项
- 字段查找请使用 `findField` 函数进行模糊匹配，不要硬编码中文字段名
- CSS 颜色必须使用 `var(--pdd-xxx)` 格式，不要使用裸 CSS 变量名
- 数值默认值**必须**使用 `??` 操作符（如 `value ?? 30`），禁止用 `||` 处理数值（`0` 是合法值）
- 成本/利润/GMV 等核心指标计算前，先搜索是否已有同样公式，避免多套实现
- 任何 filter 跳过数据时，必须计数并告知用户（禁止静默失败）
- 局部更新配置时，必须先读取已有值再合并，防止覆盖用户个性化设置
